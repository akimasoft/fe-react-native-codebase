export interface Colors {
  primary: string;
  warning: string;
  danger: string;
  success: string;
  background_color: string;
}

export enum RadiusSizes {
  small = 5,
  medium = 10,
  large = 15,
  extra_large = 20,
}
